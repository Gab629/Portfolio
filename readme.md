````markdown
# Portfolio

This is a personal portfolio project built with **HTML**, **Sass**, and **JavaScript**.

---

## 📦 Prerequisites

Before you start, make sure you have installed:

- [Node.js](https://nodejs.org/) (includes npm)
- npm (Node Package Manager)

Check your versions:

```bash
node -v
npm -v
````

---

## 📁 Project Structure

```
portfolio/
├─ css/                # Compiled CSS (generated by Sass)
│  ├─ main.css.map
├─ styles/             # Sass source files
│  ├─ components/      # Component-specific styles
│  │  ├─ header.scss
│  │  ├─ hero.scss
│  │  └─ nav.scss
│  ├─ elements/        # Basic element styles
│  │  ├─ body.scss
│  │  └─ buttons.scss
│  ├─ generics/        # Global styles like reset
│  │  └─ reset.scss
│  ├─ settings/        # Variables and config
│  │  └─ variables.scss
│  └─ main.scss        # Main Sass entry point
├─ index.html
├─ package.json
├─ package-lock.json
├─ .gitignore
└─ README.md
```

---

## 🚀 Installation

Clone the project:

```bash
git clone https://github.com/Gab629/Portfolio.git
cd Portfolio
```

Install dependencies:

```bash
npm install
```

---

## 🎨 Compile Sass and Watch

```bash
npm run sass
```

This command will:

* Compile `styles/main.scss` into `css/main.css`
* Watch for changes in all `.scss` files

---

## ⛔ Stop the watcher

Press `CTRL + C` in your terminal to stop the Sass watcher.

---

## 🧠 Recommended Workflow

1. Run `npm run sass` to start the watcher.
2. Edit `.scss` files inside `styles/`:

   * `components/` → component-specific styles
   * `elements/` → basic HTML elements
   * `generics/` → global styles (like reset)
   * `settings/` → variables and config
3. Let Sass compile automatically into `css/`.
4. Open `index.html` in your browser to see live updates.

> Remember: Only `.scss` files are tracked in Git. The compiled CSS in `css/` is **not committed**.

---

## 📌 Tech Stack

* **HTML** – Structure of the portfolio
* **Sass** – Modular, maintainable CSS preprocessor
* **JavaScript** – Interactive elements

---

## ⚠️ Notes

* Always edit `.scss` files, not compiled CSS.
* Use the watcher (`npm run sass`) to generate CSS automatically.
* The `css/` folder is ignored by Git to keep the repository clean.


